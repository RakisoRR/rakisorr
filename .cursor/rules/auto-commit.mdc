---
description: Commit meaningful changes automatically without asking
alwaysApply: true
---

# Automatic commits

When you finish a coherent unit of work that changes project files, **commit it yourself** without asking for permission.

## When to commit

- After completing a feature, fix, refactor, or docs update the user asked for
- After a batch of related edits that leave the repo in a good state
- Prefer small, focused commits over one giant dump at the end of a long session

## When not to commit

- Mid-debug with broken/incomplete changes
- Secrets or local-only files (`.env`, credentials, tokens)
- Trivial WIP the user is still iterating on in the same turn (wait until the turn’s goal is done)

## How

Follow the repo’s commit protocol: status + diff + log style, stage relevant files only, HEREDOC commit message focused on why, never amend unless the usual amend rules allow it, never push unless asked, never update git config.
